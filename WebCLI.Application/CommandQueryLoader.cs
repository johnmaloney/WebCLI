using System;

namespace WebCLI.Application
{
    /// <summary>
    /// Application level object that takes a list of commands and queries to load into the systems repositories.
    /// </summary>
    public class CommandQueryLoader
    {
        public CommandQueryLoader()
        {

        }
    }
}
